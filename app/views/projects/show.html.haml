#{@project.name} #{@project.total}
%ul
	- for expense in @expenses
		%li
			#{expense.name} | #{expense.amount}
			=link_to 'edit', edit_expense_path(expense)
			=link_to 'delete', expense, :method => :delete, :confirm => 'Are you sure?', title: "Delete"